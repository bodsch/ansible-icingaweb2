; {{ ansible_managed }}

{% if icingaweb_auth_backend is defined and icingaweb_auth_backend | count != 0 -%}
{% for key  in icingaweb_auth_backend -%}
[{{ key }}]
{% for k,v in icingaweb_auth_backend[key].items() -%}
{{ "%-10s" | format(k,) }} = "{{v }}"
{% endfor %}

{% endfor %}
{% endif %}
